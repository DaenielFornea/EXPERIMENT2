#include <iostream>
#include <iomanip>
#include <conio.h>

using namespace std;

int main()
{
   
int number, i;

    cout << "Enter a Number: "; 
    cin >> n;
 
	  cout << "Multiplication table of: " << number << endl;

    for (i=1; number <= 10; ++i) 
	{
	    cout << number << " x " << i << " = " << number * i << endl;
  }
  
_getch();
  return 0;
}
